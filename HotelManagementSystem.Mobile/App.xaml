<?xml version="1.0" encoding="UTF-8" ?>
<Application
    x:Class="HotelMobileApp.App"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:HotelMobileApp.Converters">

    <Application.Resources>
        <ResourceDictionary>

            <!-- КОЛЬОРИ -->
            <Color x:Key="AppBackground">#050818</Color>
            <Color x:Key="CardBackground">#111628</Color>
            <Color x:Key="AccentYellow">#FFD437</Color>
            <Color x:Key="AccentPurple">#4B1E71</Color>
            <Color x:Key="TextPrimary">#FFFFFF</Color>
            <Color x:Key="TextSecondary">#B8C0D0</Color>
            <Color x:Key="TextSecondaryColor">#9CA3AF</Color>
            <Color x:Key="DangerRed">#E60023</Color>

            <!-- СТИЛЬ ДЛЯ ВСІХ СТОРІНОК (де не переопреділено явно) -->
            <Style TargetType="ContentPage">
                <Setter Property="BackgroundColor" Value="{StaticResource AppBackground}" />
                <Setter Property="Padding" Value="0" />
            </Style>

            <!-- ТЕКСТОВІ СТИЛІ -->
            <Style x:Key="TitleLarge" TargetType="Label">
                <Setter Property="FontSize" Value="28" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="{StaticResource TextPrimary}" />
            </Style>

            <Style x:Key="BodyText" TargetType="Label">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="TextColor" Value="{StaticResource TextSecondary}" />
            </Style>

            <!-- ОСНОВНА КНОПКА (велика, жовта) -->
            <Style x:Key="PrimaryButton" TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource AccentYellow}" />
                <Setter Property="TextColor" Value="#000000" />
                <Setter Property="CornerRadius" Value="16" />
                <Setter Property="HeightRequest" Value="52" />
                <Setter Property="FontAttributes" Value="Bold" />
            </Style>

            <!-- КАРТОЧКИ -->
            <Style x:Key="CardFrameStyle" TargetType="Frame">
                <Setter Property="BackgroundColor" Value="{StaticResource CardBackground}" />
                <Setter Property="CornerRadius" Value="18" />
                <Setter Property="Padding" Value="16" />
                <Setter Property="HasShadow" Value="False" />
            </Style>

            <!-- АКЦЕНТНА КНОПКА (Search / Book / Plan) -->
            <Style x:Key="AccentButtonStyle" TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource AccentYellow}" />
                <Setter Property="TextColor" Value="#000000" />
                <Setter Property="CornerRadius" Value="24" />
                <Setter Property="HeightRequest" Value="48" />
                <Setter Property="FontAttributes" Value="Bold" />
            </Style>

            <!-- КОНВЕРТЕРИ -->
            <converters:BoolToColorConverter x:Key="BoolToColorConverter" />
            <converters:NotificationIconConverter x:Key="NotificationIconConverter" />

        </ResourceDictionary>
    </Application.Resources>
</Application>
